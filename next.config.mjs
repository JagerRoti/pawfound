/** @type {import('next').NextConfig} */
const nextConfig = {
  // Images in /public/uploads are served locally — no special config needed
  // The `api.bodyParser` option only applies to Pages Router (not App Router)
};

export default nextConfig;
